# ==============================================================================
# MedCare Pharma SCM Control Tower Environment Configuration
# ==============================================================================

# Option 1: Individual PostgreSQL Connection Parameters (Recommended for pgAdmin / local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medcare_scm
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=public

# Option 2: Full Database Connection URL (Override)
# For PostgreSQL: postgresql+asyncpg://postgres:postgres@localhost:5432/medcare_scm
# For Local SQLite: sqlite+aiosqlite:///./medcare_scm.db
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/medcare_scm

# API & Server Configuration
HOST=0.0.0.0
PORT=8000
SECRET_KEY=medcare-scm-control-tower-secret-key-2026

# Machine Learning Subsystem
MODEL_PATH=backend/app/ml/saved_models/demand_forecast_model.pkl
ML_RETRAIN_ON_STARTUP=false
ML_CONFIDENCE_LEVEL=0.87
